# Silver Saint server list (backup)

`servers.json` is a copy of the live Silver Saint server list, refreshed every 10 minutes
by GitHub Actions. The launcher reads it only when the main list is unreachable.
